using UnityEngine;
using UnityEngine.UI;

public class SpaceshipUI : MonoBehaviour
{
    //public Text velocityText;
    //public Text accelerationText;

    //private Rigidbody rb;
    //private Vector3 prevVelocity;

    //private void Start()
    //{
    //    // Get the Rigidbody component of the spaceship GameObject
    //    rb = GetComponent<Rigidbody>();
    //}

    //private void Update()
    //{
    //    // Get the current velocity of the spaceship in world space
    //    Vector3 velocity = rb.velocity;

    //    // Calculate the change in velocity over time
    //    Vector3 acceleration = (velocity - prevVelocity) / Time.deltaTime;

    //    // Update the UI text elements with the current velocity and acceleration values
    //    velocityText.text = $"Velocity: ({velocity.x}, {velocity.y}, {velocity.z})";
    //    accelerationText.text = $"Acceleration: ({acceleration.x}, {acceleration.y}, {acceleration.z})";

    //    // Update the previous velocity for the next frame
    //    prevVelocity = velocity;
    //}
}
